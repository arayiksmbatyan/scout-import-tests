name: Push

on:
  push:
    branches: [master]

jobs:
  scout-import-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Create MySQL Database
        run: |
          sudo apt update && sudo apt install mysql-server
          sudo mysql -e "ALTER USER 'default'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root';"
#          mysql -udefault -proot -e 'CREATE SCHEMA `test` CHARACTER SET utf8 COLLATE utf8_general_ci; GRANT ALL ON `test`.* TO default@localhost IDENTIFIED BY "root"; FLUSH PRIVILEGES;'
